# --- Postgres (local: `pnpm db:up` + `pnpm db:push`; prod: Neon) ---
DATABASE_URL="postgresql://concord:concord@localhost:5432/concord"

# --- Vercel Blob (upload storage) ---
# From concord/:  vercel link  →  vercel blob create-store concord-uploads --access private --yes  →  vercel env pull
BLOB_READ_WRITE_TOKEN=""

# --- xAI / Grok (get the key + credits at the event) ---
XAI_API_KEY=""
