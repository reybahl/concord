# --- Local Postgres (run `pnpm db:up` first) ---
DATABASE_URL="postgresql://concord:concord@localhost:5432/concord"

# --- xAI / Grok (get the key + credits at the event) ---
# Without this set, the app still runs and uses deterministic mock pipeline output,
# so the demo never hard-fails on a missing key.
XAI_API_KEY=""
